# Job Openings Board

A modern, responsive, and clean Job Board UI built using **React.js** and **Pure CSS**. This project dynamically renders job listings from an array of objects using React props and a flexible layout system.
